3. 60K 长请求 Chunked Prefill 分析
单条 ~60K token 请求的 prefill 被拆成 8 轮 chunk：

轮次	Tokens	开始时间	_model_forward 耗时
1	8192	14:41:25	~1s (首次编译)
2	8192	14:41:26	~45s
3	8192	14:42:14	~52s
4	8192	14:43:06	~42s
5	8192	14:43:50	~41s
6	8192	14:44:33	~36s
7	8192	14:45:12	~42s
8	2661	14:45:54	-
总 prefill 时间: ~4.5 分钟
每轮间隔: 仅 2-3 秒用于调度 + KV transfer → 瓶颈在模型计算，非 KV 传输
4. 并行度=1 的根因确认
每轮调度逻辑:
  ① RUNNING 请求（正在 chunked prefill 的 60K 请求）
     → 需要 min(剩余tokens, 8192) = 8192 tokens
     → token_budget: 8192 → 0（耗尽）
  ② WAITING 请求
     → budget=0 → 无法调度任何新请求
根因：max_num_batched_tokens=8192，每轮 chunked prefill 吃满全部 token budget，其他请求在整个 prefill 期间（8 轮 × ~45s = ~4.5 分钟）完全无法被调度。

5. 排除项
KV Cache 容量：1,392,640 tokens，可容纳 ~23 个 60K 请求，不是瓶颈
KV Transfer：每轮仅 2-3 秒，不是瓶颈
PCP 通信死锁：不存在，所有 rank 正常完成
Worker 异常：不存在
