# 1. 调度器每轮调度的概况
grep "\[PCP-DEBUG\]" prefill_node.log | grep "schedule() start\|waiting scheduling" | tail -30

# 2. 看 chunked prefill 分了多少轮（每次 execute_model 的 tokens 数）
grep "\[PCP-WORKER\] execute_model START pcp=0 " prefill_node.log | tail -30

# 3. 总耗时（第一条和最后一条日志的时间差）
grep "\[PCP-WORKER\] execute_model START pcp=0 " prefill_node.log | head -1
grep "\[PCP-WORKER\] after compute_logits pcp=0" prefill_node.log | tail -1
